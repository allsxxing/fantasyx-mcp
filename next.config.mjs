/** @type {import('next').NextConfig} */
const nextConfig = {
  // The MCP routes are the product; there is no image pipeline or heavy UI here.
  reactStrictMode: true,
};

export default nextConfig;
